import React from 'react';

const resumeStyle = {
  padding: '1rem',
  border: '1px solid black'
};

const Resume = () => {
  return (
    <section style={resumeStyle}>
      <h2>Resume</h2>
      <p> <a href="https://www.google.com">Resume</a>.</p>
    </section>
    );
};

export default Resume;
